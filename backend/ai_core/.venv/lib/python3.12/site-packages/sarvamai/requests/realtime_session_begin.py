# This file was auto-generated by Fern from our API Definition.

import typing

import typing_extensions
from .realtime_session_begin_config import RealtimeSessionBeginConfigParams


class RealtimeSessionBeginParams(typing_extensions.TypedDict):
    """
    Sent immediately after the connection is accepted. The `config` block
    echoes the resolved surface. Note that the endpointing field is echoed as
    `turn_detection` (`vad` / `manual`), not `endpointing`.
    """

    event: typing.Literal["session.begin"]
    request_id: str
    """
    Per-connection correlation id (quote it in support/debugging)
    """

    config: typing_extensions.NotRequired[RealtimeSessionBeginConfigParams]
    """
    Resolved server-side session configuration
    """
