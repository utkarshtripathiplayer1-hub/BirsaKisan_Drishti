# This file was auto-generated by Fern from our API Definition.

import typing

import typing_extensions


class RealtimeTranscriptFinalParams(typing_extensions.TypedDict):
    """
    Emitted once per utterance when finalization completes by the
    non-streaming finalizer. No `confidence` and no `words` array are emitted
    by this endpoint, even with `return_timestamps=true`.
    """

    event: typing.Literal["transcript.final"]
    utterance_idx: int
    """
    Zero-based utterance counter for the session
    """

    text: str
    """
    Final transcript text for the utterance
    """

    language: typing_extensions.NotRequired[str]
    """
    Detected language (BCP-47, e.g. `gu-IN`). Present only when
    `language_code=auto` was selected on the connection.
    """

    language_confidence: typing_extensions.NotRequired[float]
    """
    Detection confidence (0.0–1.0). Present only when
    `language_code=auto` was selected on the connection.
    """

    start_s: typing_extensions.NotRequired[float]
    """
    Segment start (seconds since stream start). Present only when
    `return_timestamps=true`.
    """

    end_s: typing_extensions.NotRequired[float]
    """
    Segment end (seconds since stream start). Present only when
    `return_timestamps=true`.
    """
