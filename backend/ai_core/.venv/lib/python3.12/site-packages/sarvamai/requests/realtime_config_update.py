# This file was auto-generated by Fern from our API Definition.

import typing

import typing_extensions
from ..types.realtime_config_update_endpointing import RealtimeConfigUpdateEndpointing
from ..types.realtime_config_update_mode import RealtimeConfigUpdateMode
from ..types.realtime_config_update_stream_type import RealtimeConfigUpdateStreamType


class RealtimeConfigUpdateParams(typing_extensions.TypedDict):
    """
    Updates session config mid-stream. `language_code`, `mode`, `prompt`,
    `stream_type`, and `endpointing` are boundary-gated: validated on arrival
    but deferred and applied at the next utterance boundary while an utterance
    is in progress, so each utterance's partials and final use one consistent
    config snapshot. The VAD detector knobs (`threshold`,
    `silence_duration_ms`, `min_speech_duration_ms`) apply immediately on the
    next VAD rebuild and are ignored under `endpointing=manual`. Changing
    `stream_type` to/from `simulated` is rejected (`invalid_config`).

    The server replies with `config.updated` (listing the applied keys), or an
    `error` with code `invalid_config` on a rejected value.
    """

    event: typing.Literal["config.update"]
    language_code: typing_extensions.NotRequired[str]
    """
    Re-arms auto-LID and resets the sticky language. Boundary-gated.
    """

    prompt: typing_extensions.NotRequired[str]
    """
    Non-streaming context hint (empty string clears it). Boundary-gated.
    """

    mode: typing_extensions.NotRequired[RealtimeConfigUpdateMode]
    """
    Non-streaming task selector. Boundary-gated.
    """

    stream_type: typing_extensions.NotRequired[RealtimeConfigUpdateStreamType]
    """
    `fast`↔`balanced` swap. Boundary-gated. Changing to/from `simulated` is rejected.
    """

    endpointing: typing_extensions.NotRequired[RealtimeConfigUpdateEndpointing]
    """
    `vad`/`manual` swap. Boundary-gated.
    """

    threshold: typing_extensions.NotRequired[float]
    """
    VAD activation threshold; applied immediately (vad only).
    """

    silence_duration_ms: typing_extensions.NotRequired[int]
    """
    End-of-turn silence; applied immediately (vad only).
    """

    min_speech_duration_ms: typing_extensions.NotRequired[int]
    """
    Minimum speech duration; applied immediately (vad only).
    """
